---

- name: Test Playbook
  gather_facts: false
  hosts: clab-evpn01-leaf2
  tasks:

    - name: Test
      nokia_ni_cnpi.srlinux.test:
        listtest:
        - one
        - two
        - two
        input:
          key: value
          other_key:
            - list
            - with
            - multiple
            - values
        complextest:
         - path: /system/information
           value:
             contact: me
         - path: /
           value:
             interface:
             - name: ethernet-1/1
               subinterface:
               - index: 0
                 type: bridged
