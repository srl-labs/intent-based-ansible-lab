- name: get Info
  hosts:
#    - clab-4l2s-l1
    - leaf
    - spine
  tasks:
    - nokia.srl.jsonrpc_get:
        paths:
        - path: /
          datastore: running
        - path: /platform/control[slot=A]/software-version
          datastore: state
      register: res
#    - set_fact:
#        sw_version: "{{ res.data.result[0].split('-')[0] }}"

    - debug:
        var: res
